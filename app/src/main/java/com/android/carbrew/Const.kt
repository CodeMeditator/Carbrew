package com.android.carbrew

interface Const {

    interface ItemViewType {
        companion object {
            const val MAX = 100
        }
    }

    interface Config {
        companion object {
            const val PAGE_SIZE = 50
        }
    }
}